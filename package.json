{
  "name": "quickkart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix server install",
    "build": "npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
